# Chave da OpenAI (obrigatória para gerar planos com IA)
OPENAI_API_KEY=sk-...

# Modelo opcional (padrão: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Demo local com planos mockados (sem API). No GitHub Pages isso já vem ligado no CI.
# NEXT_PUBLIC_DEMO_MODE=true
